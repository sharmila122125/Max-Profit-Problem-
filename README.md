# Max Profit Visual Gallery
Streamlit app to compute optimal building mix (Theatre, Pub, Commercial Park) and visualize results.

Run locally:
1. python -m venv env
2. env\Scripts\activate   (Windows)  OR  source env/bin/activate (Mac/Linux)
3. pip install -r requirements.txt
4. streamlit run streamlit_max_profit.py
